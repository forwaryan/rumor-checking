# Demo Replay Draft

更新时间：2026-03-26（Asia/Shanghai）

## 当前用途

这里仍只保留 replay 草案目录，不代表仓库已经公开支持 replay 能力。

## 当前结论

- `data/demos/replays/` 仍是后续 replay 文件的预留落点
- 当前没有公开 `POST /api/v1/replay` 接口
- 当前前端演示不再依赖本地 demo payload 或本地报告 JSON

## 当前目录

```text
data/
  demos/
    README.md
    replays/
      .gitkeep
```

## 当前边界

- 这里是后续 replay 的草案目录，不是已经落地的产品能力说明
- 如果未来要补 replay，优先先定接口和术语，再补文件格式
